spring:
  application:
    name: BookingSystem

  profiles:
    active: dev,uat,prod



  datasource:
    url: jdbc:mysql://localhost:3306/bookMyShow
    username: root
    password: Sanjay@098
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      minimum-idle: 10
      maximum-pool-size: 100

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: sanju.watson0110@gmail.com
    password: rtom quqr ecwf xzvb   # 16 digit app password

    properties:

      mail:
        smtp:
          auth: true
          starttls:
            enable:


server:
  port: 4000

